// build.gradle.kts (Project Root)

plugins {
    // Apply the Android Application plugin and Kotlin plugin to the root project

    id("com.android.application") version "8.13.0" apply false
    id("org.jetbrains.kotlin.android") version "1.9.20" apply false

    // Add other essential plugins if you had any (like serialization or Hilt)
}
